//This file is used for application developer to customize the packaged fiori client logic.

//To do so just uncomment the related method at below, and the implementation of the method will replace the fiori client default behavior 

fiori_client_custom = {
	//error callback for createStores method, which is defined in contentplace.js
    /*
    onCreateStoreError : function(error){
        sap.FioriClient.resetSettings(); //reset the registration
	}
	*/

}
